name: csv_ya
description: Yet another CSV parser that you may want. Supports tolerant quate escaping and treaming conversions.
version: 2.4.0
homepage: https://github.com/kkazuo/csv_ya

environment:
  sdk: ">=3.0.0 <4.0.0"

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  test: ^1.16.0
  very_good_analysis: ^4.0.0+1
dependencies:
